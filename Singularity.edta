Bootstrap:docker
From:continuumio/miniconda3

%files
    edta.yml

%post
    . /opt/conda/etc/profile.d/conda.sh
    conda activate
    conda env create -f edta.yml -n edta
    conda activate edta

%runscript
    . /opt/conda/etc/profile.d/conda.sh
    conda activate edta
    exec "$@"
